<div class="page-header"><h1>Listing Places</h1></div>

<div class='form-inline form-group'>
  <input type='text' class='form-control' id='bgh-place-filter' placeholder='Type the place name and press Enter'></input>
  <%= button_to 'New Place', new_place_path, class: 'btn btn-info', style: 'display: inline' %>
</div>

<hr>

<div class="grid places-grid">
  <% @places.each do |place| %>
    <%= content_tag :div, class: "grid-item place-grid", name: place.name do -%>
      <% image_tag asset_path('no_image.png') %>
      <div>
        <%= link_to place.name, place %>
      </div>
    <% end %>
  <% end %>
</div>
